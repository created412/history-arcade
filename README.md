# 시간여행 역사(驛舍)

‘미디어와 역사수업 실천모임’ 선생님들이 직접 만든 역사 게임을 올리고, 고치고, 함께 즐기는 정거장입니다.
역사(歷史)와 역사(驛舍)의 말장난에서 출발해, 게임은 시대로 떠나는 **열차**, 시대 분류는 **노선과 역**이 됩니다.

- 배포 주소: https://history-arcade.onrender.com

## 노선과 역 (시대 분류)

| 노선 | 역 (판) |
| --- | --- |
| 한국사선 | 1판 선사·고조선, 2판 삼국·남북국, 3판 고려, 4판 조선, 5판 근대, 6판 현대 |
| 동양사선 | 7판 동양고대사, 8판 동양중세사, 9판 동양근대사, 10판 동양현대사 |
| 서양사선 | 11판 서양고대사, 12판 서양중세사, 13판 서양근세사, 14판 서양근대사, 15판 서양현대사 |
| 환승역 | 여러 시대 (여러 노선을 오가는 게임) |

노선과 역은 `server.js`의 `LINES`에서 바꿀 수 있고, 판 번호는 순서대로 자동으로 붙습니다. 게임의 시대 값은 `노선/역`(예: `서양사/중세`)으로 저장되며,
예전 분류(`조선`, `세계사`, `동양사/중국사` 등)로 저장된 게임은 서버가 켜질 때 새 분류(또는 ‘여러 시대’)로 자동으로 옮겨집니다.

## 켜는 법

- **Windows**: `시간여행 역사 켜기.bat`을 더블클릭하면 서버가 켜지고 브라우저가 열립니다. 창을 닫으면 꺼집니다.
- **터미널**: `npm start` (또는 `node server.js`) 뒤 http://localhost:3000 을 엽니다.

설치할 패키지는 없습니다(Node 18 이상). 포트를 바꾸려면 `PORT=8080 npm start`.

교사 비밀번호 같은 설정은 **`.env` 파일**에 적습니다. 이 파일은 git에 올라가지 않습니다. 저장소가 공개이므로 비밀번호를 코드나 README에 적지 마세요.

```
TEACHER_PASSWORD=모임 교사 비밀번호
```

## 할 수 있는 일

- **승차권 끊기**: 승차권 카드나 출발 안내판에서 게임을 고르고 ‘개찰하고 타기’를 누르면 게임이 열립니다.
- **새 열차 편성하기** (선생님): 게임 이름, 만든 선생님, 시대, 소개, 하는 방법을 적고
  - 한 개짜리 **HTML 파일**(기본 100MB까지, 진행률 표시)을 올리거나
  - 스크래치·구글 사이트·Genially 같은 **링크**를 붙입니다.
  - 차비와 클리어 보상, 표지 그림(선택), **수정용 비밀번호**를 정합니다.
- **고치기 / 운행 끝내기** (선생님): 올릴 때 정한 비밀번호로 내용·파일·비밀번호를 바꾸거나 게임을 내립니다.
- **노선도에서 고르기, 찾기**: 노선 이름을 누르면 그 노선 전체, 역을 누르면 그 시대의 게임만 보입니다.

## 엽전: 차비와 클리어 보상

| 승객 | 들어오는 법 | 엽전 |
| --- | --- | --- |
| 선생님 | 모임 공용 교사 비밀번호 | 정기권: 엽전 없이 탑니다. 열차 편성과 역무실(반 관리)을 씁니다. |
| 학생 | 반 코드 6자리 + 이름 + 숫자 4자리 비밀번호 | 처음 들어오면 **3닢**. 차비를 내고 타고, 클리어하면 보상을 받습니다. |
| 구경꾼 | 들어오지 않음 | 무료 열차만 탈 수 있습니다. |

- **차비와 보상**은 게임을 편성할 때 선생님이 정합니다. (차비 0~5닢, 기본 1닢 / 보상 0~10닢, 기본 2닢)
- **링크 게임**은 클리어 신호를 보낼 수 없어 항상 무료 열차입니다.
- **역무실**에서 반을 만들면 반 코드가 나옵니다. 반별 학생의 엽전, 클리어 횟수, 마지막으로 탄 날을 보고,
  비밀번호를 잊은 학생에게 새 비밀번호를 정해 줄 수 있습니다.

### 게임에 클리어 신호 넣기

HTML 게임에서 학생이 게임을 깼을 때 아래 한 줄이 실행되게 하면 보상이 들어갑니다.

```js
parent.postMessage({ type: 'history-station:clear' }, '*');
```

- 한 번 탈 때 보상은 한 번만 받습니다.
- 개찰 뒤 10초가 지나기 전에 온 신호는 받지 않습니다.
- 학생이 브라우저 개발자 도구로 신호를 흉내 낼 수는 있지만, 그래도 차비를 내고 10초를 기다려야 합니다.
  수업용 동기 부여 장치로 생각해 주세요.

## 파일 구조

| 경로 | 내용 |
| --- | --- |
| `server.js` | 의존성 없는 Node 서버 (API + 정적 파일 + 게임 실행) |
| `passengers.js` | 교사·학생 로그인, 반, 엽전, 탑승·클리어 기록 |
| `github-store.js` | GitHub 브랜치를 데이터 보관소로 쓰는 모듈 |
| `render.yaml` | Render 배포 설정 |
| `public/` | 정거장 화면 (HTML·CSS·JS, 승차권 아이콘) |
| `seed/` | 처음 켤 때 들여놓는 견본 게임 (비밀번호 `1234`) |
| `data/` | 실제 저장소. `games.json`, `classes.json`(반·학생·엽전), 올라온 HTML 게임 파일 (git에서 제외) |

## 안전장치

- 올라온 HTML 게임은 `Content-Security-Policy: sandbox`로 격리된 출처에서 실행되어,
  사이트 데이터나 다른 선생님의 게임에 손대지 못합니다.
- 게임 비밀번호와 학생 비밀번호는 scrypt 해시로만 저장하고 화면으로 내보내지 않습니다.
- 교사 비밀번호는 10분에 8번까지만 틀릴 수 있습니다.
- 엽전과 승차 기록은 2분마다 모아서, 반 만들기·학생 등록은 바로 GitHub에 보관합니다.
- 백업은 `data/` 폴더를 통째로 복사하면 됩니다.

## 인터넷에 올리기 (Render 무료 + GitHub 보관)

무료 호스팅은 재시작 때 디스크가 비워지므로 데이터를 GitHub에 보관합니다. 게임을 올려도 재배포되지 않습니다.

- 게임 목록·반·엽전 기록(`games.json`, `classes.json`): `arcade-data` 브랜치에 커밋
- HTML 게임 파일: `arcade-data-files` 릴리스의 첨부 파일 (원본 그대로, 파일당 2GB까지). 서버는 파일을 받은 즉시 선생님께 응답하고 GitHub 보관은 뒤에서 이어서 합니다.
- 서버가 켜질 때 브랜치와 릴리스에서 모두 내려받습니다. 릴리스 `arcade-data-files`는 직접 지우지 마세요.

1. **GitHub 토큰 만들기**: GitHub → Settings → Developer settings → Fine-grained tokens → Generate
   - Repository access: *Only select repositories* → `history-arcade`
   - Permissions: *Contents* → **Read and write**
2. **Render에 연결하기**: [Deploy to Render](https://render.com/deploy?repo=https://github.com/created412/history-arcade)
   - `render.yaml` 설정대로 무료 웹 서비스가 만들어집니다.
   - `GITHUB_TOKEN` 칸에 1번 토큰을, `TEACHER_PASSWORD` 칸에 모임 교사 비밀번호를 넣고 *Apply*.
3. 몇 분 뒤 `https://history-arcade-xxxx.onrender.com` 같은 주소가 나오면 선생님들께 공유합니다.

무료 플랜은 15분 동안 접속이 없으면 잠들어, 다음 첫 접속에 30~50초가 걸립니다.

| 환경변수 | 뜻 |
| --- | --- |
| `GITHUB_TOKEN` | 저장소 Contents 쓰기 권한 토큰 (없으면 로컬 `data/` 폴더만 사용) |
| `GITHUB_REPO` | 보관할 저장소, 예: `created412/history-arcade` |
| `DATA_BRANCH` | 보관 브랜치 (기본 `arcade-data`) |
| `TEACHER_PASSWORD` | 모임 선생님들이 함께 쓰는 교사 비밀번호 (배포 환경에서 없으면 교사로 들어올 수 없음) |
| `SESSION_SECRET` | 로그인 토큰 서명용 비밀값 (Render 블루프린트가 자동 생성) |
| `MAX_GAME_MB` | HTML 게임 파일 한 개의 최대 크기 (기본 100) |
| `DATA_DIR` | 로컬 데이터 폴더 위치 (기본 `./data`) |
